const chai = require( "chai" );
chai.should();
const electron = require( "electron" );
const app = electron.app;

describe( "app tests", () => {
    it( "should do something", () => {
        
    } );
} );